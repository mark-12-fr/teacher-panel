# FastAPI backend base URL (no trailing slash). Local default: http://127.0.0.1:5001
NEXT_PUBLIC_API_BASE=http://127.0.0.1:5001

# Supabase (Auth only — public values). Defaults to the AcadTrack project if unset.
NEXT_PUBLIC_SUPABASE_URL=https://njzvuwkepaasnsvuujgx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
